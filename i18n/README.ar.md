[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)

<p align="center">
  <img src="https://raw.githubusercontent.com/lachlanchen/lachlanchen/main/figs/banner.png" alt="LazyingArt banner" />
</p>

# OrganoidAgent

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![Backend](https://img.shields.io/badge/Backend-Tornado-2c7fb8)
![Frontend](https://img.shields.io/badge/Frontend-PWA-0a9396)
![Status](https://img.shields.io/badge/Status-Active-success)
![Data](https://img.shields.io/badge/Data-Local%20first-4c956c)
![Preview](https://img.shields.io/badge/Preview-Multi--format-f4a261)

OrganoidAgent هو خلفية خفيفة مبنية على Tornado مع واجهة أمامية Progressive Web App (PWA) لتصفّح ومعاينة مجموعات بيانات الأورغانويد محليًا. يدعم معاينات عملية تراعي نوع الملف للجداول، وصور المجهر (بما فيها TIFF)، والأرشيفات، وملفات النص المضغوطة بصيغة gzip، وكائنات التحليل AnnData بصيغة `.h5ad`.

## نظرة عامة 🔭

صُمّم التطبيق الأساسي لاستكشاف تفاعلي لمجموعات البيانات بأقل إعداد ممكن:

- واجهة API الخلفية ومحرك المعاينة في `app.py`
- واجهة PWA في `web/`
- أدوات المساعدة للتنزيل في `scripts/`
- مساحة عمل البيانات المحلية في `datasets/` (غير متتبعة في git)

يحتوي هذا المستودع أيضًا على مساحات عمل بحثية وأدوات مجاورة (`BioAgent`, `BioAgentUtils`, `references`, `results`, و`papers` كـ submodule). بيئة التشغيل الأساسية الموصوفة في هذا README هي تطبيق `OrganoidAgent` في المستوى الأعلى.

## الميزات ✨

- فهرسة محلية لمجموعات البيانات مع ملخصات الحجم وعدد الملفات
- سرد تكراري لملفات مجموعة البيانات مع استنتاج نوع الملف
- دعم معاينة يشمل جداول CSV/TSV/XLS/XLSX
- دعم معاينة يشمل صور TIFF/JPG/PNG
- دعم معاينة يشمل ملخصات `.h5ad` مع توليد معاينة مبعثرة embedding/PCA
- دعم معاينة يشمل سرد أرشيفات ZIP/TAR/TGZ + محاولة معاينة أول صورة
- دعم معاينة يشمل أول أسطر النص في ملفات `.gz`
- نقطة نهاية لاستخراج الأرشيفات لمجموعات البيانات المعبأة الكبيرة
- بطاقات بيانات وصفية على مستوى مجموعة البيانات تُعرض من Markdown
- واجهة PWA مع service worker وmanifest
- تنقية أساسية للمسارات (`safe_dataset_path`) لحصر الوصول إلى الملفات داخل `datasets/`

### لمحة سريعة

| المجال | ما الذي يوفّره |
|---|---|
| اكتشاف مجموعات البيانات | سرد مجموعات بيانات على مستوى المجلد مع عدد الملفات وملخصات الحجم |
| استكشاف الملفات | سرد تكراري واستنتاج النوع (`image`, `table`, `analysis`, `archive`, إلخ) |
| معاينات غنيّة | جداول، TIFF/صور، مقتطفات نص gzip، محتويات الأرشيف، ملخصات AnnData |
| المرئيات التحليلية | معاينات مبعثرة `.h5ad` من تضمينات `obsm` أو بديل PCA |
| دعم الحِزم | سرد الأرشيف + نقطة نهاية استخراج لحزم الضغط الكبيرة |
| تجربة الويب | PWA قابل للتثبيت مع أصول service worker ملائمة للعمل دون اتصال |

## بنية المشروع 🗂️

```text
OrganoidAgent/
├─ app.py
├─ web/
│  ├─ index.html
│  ├─ app.js
│  ├─ styles.css
│  ├─ sw.js
│  ├─ manifest.json
│  └─ icons/
├─ scripts/
│  ├─ download_organoid_datasets.py
│  ├─ download_drug_screening_datasets.py
│  └─ overlay_segmentations.py
├─ datasets/                      # downloaded data and preview cache (git-ignored)
├─ metadata/
│  └─ zenodo_10643410.md
├─ papers/                        # submodule: prompt-is-all-you-need
├─ i18n/                          # currently present for multilingual README files
├─ BioAgent/                      # related but separate app
├─ BioAgentUtils/                 # related training/data utilities
├─ references/
├─ results/
└─ vendor/                        # external submodules (copilot-sdk, paper-agent, codex)
```

## المتطلبات المسبقة ✅

- Python `3.10+`
- مدير بيئة موصى به: `conda` أو `venv`

الحزم المطلوبة/الاختيارية في Python كما يمكن استنتاجها من الشيفرة المصدرية:

| الحزمة | الدور |
|---|---|
| `tornado` | مطلوبة لبدء الخادم |
| `pandas` | اختيارية: دعم معاينة الجداول |
| `anndata`, `numpy` | اختيارية: معاينة `.h5ad` ورسوم التحليل |
| `Pillow` | اختيارية: عرض الصور والمعاينات المُولّدة |
| `tifffile` | اختيارية: دعم معاينة TIFF |
| `requests` | اختيارية: سكربتات تنزيل مجموعات البيانات |
| `kaggle` | اختيارية: تنزيلات Kaggle في سكربت drug-screening |

ملاحظة افتراضية: لا يوجد حاليًا ملف `requirements.txt` أو `pyproject.toml` أو `environment.yml` في الجذر للتطبيق الرئيسي.

## التثبيت ⚙️

```bash
cd /home/lachlan/ProjectsLFS/OrganoidAgent

# Option A: conda (example)
conda create -n organoid python=3.10 -y
conda activate organoid
pip install tornado pandas anndata numpy pillow tifffile requests

# Option B: minimal runtime only
pip install tornado
```

## الاستخدام 🚀

### البدء السريع

```bash
cd /home/lachlan/ProjectsLFS/OrganoidAgent
conda activate organoid  # optional if you already have the deps
python app.py --port 8080
```

افتح `http://localhost:8080`.

### اختبار API سريع

```bash
curl http://localhost:8080/api/datasets
```

### تنزيل البيانات (اختياري)

```bash
python scripts/download_organoid_datasets.py
python scripts/download_drug_screening_datasets.py
```

تُحفَظ البيانات المنزّلة في `datasets/` (غير متتبعة في git).

## نقاط نهاية API 🌐

| Method | Endpoint | Purpose |
|---|---|---|
| `GET` | `/api/datasets` | List datasets with summary stats |
| `GET` | `/api/datasets/{name}` | List files for one dataset |
| `GET` | `/api/datasets/{name}/metadata` | Return markdown metadata card |
| `GET` | `/api/category/{datasets|segmentation|features|analysis}` | Category-oriented file listing |
| `GET` | `/api/preview?path=<relative_path_under_datasets>` | File-type-aware preview payload |
| `POST` | `/api/extract?path=<archive_relative_path_under_datasets>` | Extract archive into sibling `_extracted` folder |
| `GET` | `/files/<path>` | Raw dataset file serving |
| `GET` | `/previews/<path>` | Generated preview asset serving |

مثال على استدعاء المعاينة:

```bash
curl "http://localhost:8080/api/preview?path=zenodo_10643410/some_file.h5ad"
```

## الإعداد 🧩

الإعداد الحالي للتشغيل صغير عمدًا:

- منفذ الخادم: الوسيط `--port` في `app.py` (القيمة الافتراضية `8080`)
- مجلد البيانات: مثبت على `datasets/` نسبةً إلى جذر المستودع
- ذاكرة تخزين المعاينات: `datasets/.cache/previews`
- ربط البيانات الوصفية: قاموس `DATASET_METADATA` في `app.py`
- رمز GitHub API لأداة التنزيل (اختياري): متغير البيئة `GITHUB_TOKEN` أو `--github-token`

ملاحظة افتراضية: إذا كنت بحاجة إلى جذر بيانات قابل للتهيئة أو إعدادات خادم إنتاج، فهذا غير مكشوف بعد في ملفات إعداد المستوى الأعلى.

## أمثلة 🧪

### تصفح الملفات حسب الفئة

```bash
curl http://localhost:8080/api/category/analysis
curl http://localhost:8080/api/category/features
```

### استخراج أرشيف

```bash
curl -X POST "http://localhost:8080/api/extract?path=zenodo_8177571/sample_archive.zip"
```

### تشغيل أوضاع تنزيل انتقائية

```bash
# Organoid datasets: skip GEO, keep Zenodo
python scripts/download_organoid_datasets.py --skip-geo

# Drug-screening datasets: only Zenodo
python scripts/download_drug_screening_datasets.py --skip-figshare --skip-github --skip-kaggle
```

## ملاحظات التطوير 🛠️

- الواجهة الخلفية تخدّم الأصول الثابتة للواجهة الأمامية من `web/`.
- يوجد service worker وmanifest في `web/sw.js` و`web/manifest.json`.
- توجيه أنواع الملفات والمعاينات مطبّق في `app.py`.
- التحقق اليدوي (إرشادات المشروع الحالية): تحميل PWA على `http://localhost:8080`
- التحقق اليدوي (إرشادات المشروع الحالية): تعيد `/api/datasets` استجابة JSON
- التحقق اليدوي (إرشادات المشروع الحالية): تعمل المعاينات لملفات CSV/XLSX/images/archives

## استكشاف الأخطاء وإصلاحها 🩺

- `ModuleNotFoundError` لمكتبات المعاينة: ثبّت الحزم الناقصة (`pandas`, `anndata`, `numpy`, `Pillow`, `tifffile`).
- قائمة مجموعات البيانات فارغة: تأكد من وجود البيانات تحت `datasets/` وأن المجلدات لا تبدأ بنقطة.
- معاينة `.h5ad` بلا صورة مبعثرة: تأكد من تثبيت `anndata` و`numpy` و`Pillow`.
- مشكلات معاينة/استخراج الأرشيفات الكبيرة: استخدم نقطة نهاية الاستخراج وافحص الملفات المستخرجة مباشرةً.
- أخطاء حد المعدل في تنزيل GitHub: وفّر `GITHUB_TOKEN` عبر متغير بيئة أو خيار CLI.
- تنزيل Kaggle لا يعمل: ثبّت `kaggle` واضبط بيانات الاعتماد في `~/.kaggle/kaggle.json`.

## خارطة الطريق 🧭

تحسينات محتملة لاحقًا (غير مطبّقة بالكامل بعد في هذا التطبيق الجذري):

- إضافة ملف اعتماديات في الجذر (`requirements.txt` أو `pyproject.toml`)
- إضافة اختبارات آلية لمعالجات API ووظائف المعاينة
- إضافة إعدادات قابلة للتهيئة لجذر البيانات والذاكرة المؤقتة
- إضافة ملف تشغيل إنتاج صريح (non-debug مع إرشادات reverse-proxy)
- توسيع التوثيق متعدد اللغات تحت `i18n/`

## المساهمة 🤝

المساهمات مرحّب بها. سير عمل عملي:

1. Fork ثم أنشئ فرعًا مركّزًا.
2. اجعل التغييرات محصورة في مجال منطقي واحد.
3. تحقّق يدويًا من بدء التطبيق ونقاط النهاية الأساسية.
4. افتح PR يتضمن ملخصًا، والأوامر التي شغّلتها، ولقطات شاشة لتغييرات الواجهة.

اتفاقيات الأسلوب المحلية في هذا المستودع:

- Python: مسافة بادئة 4 فراغات، دوال/ملفات snake_case، وفئات CapWords
- أبقِ منطق الواجهة الأمامية في `web/app.js` لهذا التطبيق (وتجنب إعادة الكتابة غير الضرورية بإطار عمل)
- اجعل التعليقات موجزة وفقط عندما يكون المنطق غير بديهي

## تخطيط المشروع (ملخص مرجعي) 📌

- `app.py`: خادم Tornado ومسارات API.
- `web/`: أصول PWA.
- `scripts/`: أدوات مساعدة لتنزيل مجموعات البيانات.
- `datasets/`: تخزين البيانات المحلية.
- `papers/`: submodule يحتوي مواد مرجعية.

## الترخيص 📄

لا يوجد حاليًا ملف `LICENSE` للمشروع في جذر هذا المستودع.

ملاحظة افتراضية: إلى أن يُضاف ترخيص في الجذر، اعتبر شروط إعادة الاستخدام/إعادة التوزيع غير محددة لشيفرة OrganoidAgent في المستوى الأعلى.

## Sponsor & Donate ❤️

- GitHub Sponsors: https://github.com/sponsors/lachlanchen
- Donate: https://chat.lazying.art/donate
- PayPal: https://paypal.me/RongzhouChen
- Stripe: https://buy.stripe.com/aFadR8gIaflgfQV6T4fw400
