[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


<p align="center">
  <img src="https://raw.githubusercontent.com/lachlanchen/lachlanchen/main/figs/banner.png" alt="LazyingArt banner" />
</p>

# OrganoidAgent

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![Backend](https://img.shields.io/badge/Backend-Tornado-2c7fb8)
![Frontend](https://img.shields.io/badge/Frontend-PWA-0a9396)
![Status](https://img.shields.io/badge/Status-Active-success)
![Data](https://img.shields.io/badge/Data-Local%20first-4c956c)
![Preview](https://img.shields.io/badge/Preview-Multi--format-f4a261)

OrganoidAgent — это легковесный backend на Tornado + frontend в формате Progressive Web App (PWA) для локального просмотра и предпросмотра наборов данных органоидов. Поддерживаются практичные предпросмотры с учетом типа файла: таблицы, микроскопические изображения (включая TIFF), архивы, текстовые файлы gzip и объекты анализа AnnData `.h5ad`.

## Обзор 🔭

Основное приложение рассчитано на интерактивное исследование датасетов с минимальной настройкой:

- Backend API и движок предпросмотра в `app.py`
- PWA frontend в `web/`
- Скрипты загрузки в `scripts/`
- Локальное рабочее пространство данных в `datasets/` (игнорируется git)

Этот репозиторий также содержит смежные исследовательские и утилитные рабочие пространства (`BioAgent`, `BioAgentUtils`, `references`, `results`, `vendor`, подмодуль `papers`). Основной runtime, описанный в этом README, — приложение верхнего уровня `OrganoidAgent`.

## Возможности ✨

- Локальная индексация датасетов с суммарной статистикой по размеру и числу файлов
- Рекурсивный список файлов датасета с определением типа файла
- Предпросмотр для таблиц CSV/TSV/XLS/XLSX
- Предпросмотр изображений TIFF/JPG/PNG
- Предпросмотр `.h5ad` со сводкой и генерацией scatter-превью по embedding/PCA
- Предпросмотр содержимого архивов ZIP/TAR/TGZ + попытка показать первое изображение
- Предпросмотр первых строк текстовых `.gz`
- Эндпоинт распаковки архивов для больших упакованных датасетов
- Карточки метаданных на уровне датасета, рендерящиеся из Markdown
- PWA frontend с service worker и manifest
- Базовая санация путей (`safe_dataset_path`), ограничивающая доступ к файлам внутри `datasets/`

### Кратко

| Область | Что предоставляет |
|---|---|
| Поиск датасетов | Список датасетов на уровне директорий с числом файлов и сводкой по размеру |
| Исследование файлов | Рекурсивный список и определение типа (`image`, `table`, `analysis`, `archive` и т.д.) |
| Расширенные предпросмотры | Таблицы, TIFF/изображения, фрагменты gzip-текста, содержимое архивов, сводки AnnData |
| Визуализация анализа | Scatter-превью для `.h5ad` из embedding в `obsm` или fallback на PCA |
| Поддержка упаковок | Список архивов + эндпоинт распаковки для больших сжатых наборов |
| Web UX | Устанавливаемый PWA с offline-friendly ресурсами service worker |

## Структура проекта 🗂️

```text
OrganoidAgent/
├─ app.py
├─ web/
│  ├─ index.html
│  ├─ app.js
│  ├─ styles.css
│  ├─ sw.js
│  ├─ manifest.json
│  └─ icons/
├─ scripts/
│  ├─ download_organoid_datasets.py
│  ├─ download_drug_screening_datasets.py
│  └─ overlay_segmentations.py
├─ datasets/                      # downloaded data and preview cache (git-ignored)
├─ metadata/
│  └─ zenodo_10643410.md
├─ papers/                        # submodule: prompt-is-all-you-need
├─ i18n/                          # currently present for multilingual README files
├─ BioAgent/                      # related but separate app
├─ BioAgentUtils/                 # related training/data utilities
├─ references/
├─ results/
└─ vendor/                        # external submodules (copilot-sdk, paper-agent, codex)
```

## Предварительные требования ✅

- Python `3.10+`
- Рекомендуемый менеджер окружений: `conda` или `venv`

Обязательные/опциональные Python-пакеты, определенные по исходному коду:

| Пакет | Роль |
|---|---|
| `tornado` | Обязателен для запуска сервера |
| `pandas` | Опционально: поддержка предпросмотра таблиц |
| `anndata`, `numpy` | Опционально: предпросмотр `.h5ad` и графики анализа |
| `Pillow` | Опционально: рендеринг изображений и сгенерированные предпросмотры |
| `tifffile` | Опционально: поддержка предпросмотра TIFF |
| `requests` | Опционально: скрипты загрузки датасетов |
| `kaggle` | Опционально: загрузки Kaggle в скрипте drug-screening |

Примечание-предположение: в корне сейчас нет `requirements.txt`, `pyproject.toml` или `environment.yml` для приложения верхнего уровня.

## Установка ⚙️

```bash
cd /home/lachlan/ProjectsLFS/OrganoidAgent

# Option A: conda (example)
conda create -n organoid python=3.10 -y
conda activate organoid
pip install tornado pandas anndata numpy pillow tifffile requests

# Option B: minimal runtime only
pip install tornado
```

## Использование 🚀

### Быстрый старт

```bash
cd /home/lachlan/ProjectsLFS/OrganoidAgent
conda activate organoid  # optional if you already have the deps
python app.py --port 8080
```

Откройте `http://localhost:8080`.

### Быстрый API-тест

```bash
curl http://localhost:8080/api/datasets
```

### Загрузка данных (опционально)

```bash
python scripts/download_organoid_datasets.py
python scripts/download_drug_screening_datasets.py
```

Загруженные данные сохраняются в `datasets/` (игнорируется git).

## API-эндпоинты 🌐

| Method | Endpoint | Purpose |
|---|---|---|
| `GET` | `/api/datasets` | Список датасетов со сводной статистикой |
| `GET` | `/api/datasets/{name}` | Список файлов одного датасета |
| `GET` | `/api/datasets/{name}/metadata` | Возврат markdown-карточки метаданных |
| `GET` | `/api/category/{datasets|segmentation|features|analysis}` | Список файлов по категориям |
| `GET` | `/api/preview?path=<relative_path_under_datasets>` | Предпросмотр с учетом типа файла |
| `POST` | `/api/extract?path=<archive_relative_path_under_datasets>` | Распаковка архива в соседнюю папку `_extracted` |
| `GET` | `/files/<path>` | Выдача сырых файлов датасета |
| `GET` | `/previews/<path>` | Выдача сгенерированных предпросмотров |

Пример запроса предпросмотра:

```bash
curl "http://localhost:8080/api/preview?path=zenodo_10643410/some_file.h5ad"
```

## Конфигурация 🧩

Текущая runtime-конфигурация намеренно минималистична:

- Порт сервера: аргумент `--port` в `app.py` (по умолчанию `8080`)
- Каталог данных: фиксирован как `datasets/` относительно корня репозитория
- Кэш предпросмотров: `datasets/.cache/previews`
- Сопоставление метаданных: словарь `DATASET_METADATA` в `app.py`
- GitHub API token для загрузчика (опционально): переменная окружения `GITHUB_TOKEN` или `--github-token`

Примечание-предположение: если вам нужны настраиваемые корни датасетов или production-настройки сервера, они пока не вынесены в конфигурационные файлы верхнего уровня.

## Примеры 🧪

### Просмотр файлов по категориям

```bash
curl http://localhost:8080/api/category/analysis
curl http://localhost:8080/api/category/features
```

### Распаковка архива

```bash
curl -X POST "http://localhost:8080/api/extract?path=zenodo_8177571/sample_archive.zip"
```

### Запуск выборочных режимов загрузки

```bash
# Organoid datasets: skip GEO, keep Zenodo
python scripts/download_organoid_datasets.py --skip-geo

# Drug-screening datasets: only Zenodo
python scripts/download_drug_screening_datasets.py --skip-figshare --skip-github --skip-kaggle
```

## Заметки по разработке 🛠️

- Backend раздает frontend-статические ресурсы из `web/`.
- Service worker и manifest находятся в `web/sw.js` и `web/manifest.json`.
- Маршрутизация по типам файлов и предпросмотры реализованы в `app.py`.
- Ручная валидация (текущие рекомендации проекта): PWA загружается по `http://localhost:8080`
- Ручная валидация (текущие рекомендации проекта): `/api/datasets` возвращает JSON
- Ручная валидация (текущие рекомендации проекта): предпросмотры работают для CSV/XLSX/images/archives

## Устранение неполадок 🩺

- `ModuleNotFoundError` для библиотек предпросмотра: установите недостающие пакеты (`pandas`, `anndata`, `numpy`, `Pillow`, `tifffile`).
- Пустой список датасетов: проверьте, что данные есть в `datasets/` и каталоги не начинаются с точки.
- Для `.h5ad` не генерируется scatter-изображение: убедитесь, что установлены `anndata`, `numpy` и `Pillow`.
- Проблемы с предпросмотром/распаковкой больших архивов: используйте эндпоинт распаковки и проверьте извлеченные файлы напрямую.
- Ошибки лимита GitHub downloader: передайте `GITHUB_TOKEN` через переменную окружения или CLI-флаг.
- Не работает загрузка Kaggle: установите `kaggle` и настройте credentials в `~/.kaggle/kaggle.json`.

## Дорожная карта 🧭

Потенциальные следующие улучшения (пока не полностью реализованы в этом корневом приложении):

- Добавить root-манифест зависимостей (`requirements.txt` или `pyproject.toml`)
- Добавить автоматические тесты для API-обработчиков и функций предпросмотра
- Добавить настраиваемые параметры корня датасетов и кэша
- Добавить явный production-профиль запуска (non-debug, рекомендации по reverse proxy)
- Расширить многоязычную документацию в `i18n/`

## Вклад 🤝

Вклад приветствуется. Практичный рабочий процесс:

1. Сделайте fork и создайте сфокусированную ветку.
2. Держите изменения в рамках одной логической области.
3. Вручную проверьте запуск приложения и ключевые эндпоинты.
4. Откройте PR с кратким описанием, списком выполненных команд и скриншотами для UI-изменений.

Локальные соглашения по стилю в этом репозитории:

- Python: отступ 4 пробела, snake_case для функций/файлов, CapWords для классов
- Для этого приложения держите frontend-логику в `web/app.js` (избегайте ненужных переписываний на фреймворки)
- Комментарии должны быть краткими и только там, где логика неочевидна

## Структура проекта (каноничное резюме) 📌

- `app.py`: Tornado-сервер и API-маршруты.
- `web/`: ресурсы PWA.
- `scripts/`: вспомогательные скрипты загрузки датасетов.
- `datasets/`: локальное хранилище данных.
- `papers/`: подмодуль с эталонными материалами.

## Лицензия 📄

В корне этого репозитория сейчас отсутствует файл `LICENSE` для проекта верхнего уровня.

Примечание-предположение: пока в корне не добавлена лицензия, считайте условия повторного использования/распространения для кодовой базы верхнего уровня OrganoidAgent неуточненными.

## Sponsor & Donate ❤️

- GitHub Sponsors: https://github.com/sponsors/lachlanchen
- Donate: https://chat.lazying.art/donate
- PayPal: https://paypal.me/RongzhouChen
- Stripe: https://buy.stripe.com/aFadR8gIaflgfQV6T4fw400
